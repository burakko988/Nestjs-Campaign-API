export enum BenefitType {
    DISCOUNT = 'DISCOUNT',
    FIXED = 'FIXED',
}
// Fixed if the selected campaign 2 buy 1 pay this is fixed type

// Discount if the selected campaign as min price 200  %50 discount this is discount type
